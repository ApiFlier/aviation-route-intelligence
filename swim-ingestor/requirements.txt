# FlightConn SWIM Ingestor — Phase 2A dependencies

# STOMP protocol client for FAA SWIM / Solace PubSub+ broker
# FAA SWIM SCDS uses Solace PubSub+, which supports STOMP over TLS (port 61614).
# stomp.py is the standard lightweight Python STOMP client.
stomp.py>=8.1.0,<9.0

# Pure-Python MySQL connector (no C build dependencies required)
PyMySQL>=1.1.0,<2.0
