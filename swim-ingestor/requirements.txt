# FlightConn SWIM Ingestor — Phase 2A dependencies

# Solace PubSub+ Python client (primary path)
solace-pubsubplus>=1.5.0

# XML parser for FAA SWIM messages
lxml>=4.9.0

# STOMP protocol client (advanced fallback)
stomp.py>=8.1.0,<9.0

# Pure-Python MySQL connector (no C build dependencies required)
PyMySQL>=1.1.0,<2.0
